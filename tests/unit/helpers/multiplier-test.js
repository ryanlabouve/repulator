import { multiplier } from '../../../helpers/multiplier';
import { module, test } from 'qunit';

module('Unit | Helper | multiplier');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = multiplier(42);
  assert.ok(result);
});
